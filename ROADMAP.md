# 路线图与开发节奏

## 当前状态：Instant Switchboard 打磨期（2026-08-10 起）

产品差异化已经收敛为：**Windows 优先 + 多 harness + 本地离线 + 近乎无感的启动与检索 + 诚实的续接能力**。

当前暂停扩张重功能，进入主路径打磨期：

- 以真实使用为主，记录摩擦点（界面拥挤、流程多点一步、体验别扭处）
- 打磨期产出的问题清单，作为解冻后迭代的输入
- 解冻原则：每个新功能须由真实使用中的摩擦点驱动，而非"想到就加"

## 体验预算

| 指标 | 目标 |
|---|---:|
| 本机页面监听 | `< 500 ms`（发布构建另行记录） |
| 已建索引后的列表请求 | `< 50 ms`（1,000 条量级） |
| 搜索主路径 | 搜索 → 选中 → 续接，不超过 3 个动作 |
| 能力表达 | 精确会话 / 工作区级 / 恢复命令 / 仅打开客户端必须分开标注 |

## 明确不竞争的方向

- 不做云端账号、团队协作平台或新的聊天客户端
- 不优先做代替 harness 发消息、批量控制 agent 的重编排
- 不为“看起来智能”默认引入 LLM 摘要成本
- 不猜测、不伪装未验证的私有会话跳转协议

## 路线图（冻结中，仅存档）

| 阶段 | 内容 | 状态 |
|---|---|---|
| **P0** | Agent 接入层：`/agent/*` 只读 API + `hub_agent.py`（CLI/MCP 双模式） | ✅ 已交付（v0.1.7） |
| **P0** | Instant Switchboard：先绑定端口、后台建索引、统一续接能力、10 个内置适配器 | ✅ 已交付（v0.2.0） |
| **P0** | WorkBuddy 精确会话、ZCode 安全工作区启动、Context Packet 本地接力包 | ✅ 已交付（v0.3.0） |
| **P0** | 显式本地记忆卡（默认不附带、可清空、可备份） | ✅ 已交付（v0.3.0） |
| **P0** | Qoder 新版标题索引 + 明文 transcript 回顾卡；EXE 内置动态端口托盘与统一退出 | ✅ 已交付（v0.4.0） |
| **P0** | 共享恢复层 `agent_recovery`：Hub 与 find-agent-data skill 共用 Qoder 映射 | ✅ 已交付 |
| **P0** | 托盘保活、诚实续接 capability、Agent handoff JSON、conversation-hub skill | ✅ 已交付 |
| **P0** | v0.4.1 双安装入口：Agent 一句话安装 + 经成品冒烟的 Windows/macOS Release | ✅ 已交付 |
| **P1** | 对话预蒸馏摘要（规则版优先；模型 API 做成可选增强，默认关闭、不填 key 即不存在） | ⏸ 观察真实使用后再决定 |
| **P1** | 版本化压缩链、候选记忆建议与跨 Agent 导出器 | ⏸ 冻结 |
| **P2** | 向量检索、图记忆、自动长期记忆、跨设备同步 | ⏸ 冻结（隐私和误记忆风险高） |
| **P3** | Agent 资产索引（PPT/文档等产物管理） | ⏸ 冻结 |

## 设计底线（解冻后依然有效）

- 本地优先：只绑定 127.0.0.1，原始数据只读
- 零第三方依赖：纯 Python 标准库
- 模型永远是可选增强，不是依赖；默认路径离线可用
